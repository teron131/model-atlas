/**
 * Time Horizon Index evidence requirements are tracked from VALS.
 *
 * Page source: https://www.vals.ai/benchmarks/time_horizon_index
 *
 * Non-scoring watchlist evidence required before implementation:
 * - a stable structured leaderboard with source model and configuration identities;
 * - task-level outcomes or distributions, uncertainty, run counts, and costs;
 * - a documented horizon aggregation rule and comparable harness conditions;
 * - current frontier coverage showing non-saturated, rank-stable separation.
 *
 * This watchlist file intentionally performs no fetch and is not imported or registered.
 */
